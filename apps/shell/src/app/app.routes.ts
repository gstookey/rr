import { type Routes } from '@angular/router';

/**
 * Empty by design in S0. Floors arrive as lazy `loadChildren` entries behind
 * `CanMatch` guards in S1+ (practical_picture_v0.md §1).
 */
export const routes: Routes = [];
