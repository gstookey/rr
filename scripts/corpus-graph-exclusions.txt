# corpus-graph exclusions — paths OUT-OF-UNIVERSE for the corpus graph.
# Created: 2026-08-25
# One repo-relative path prefix or glob per line; each entry is preceded by a
# `#` comment saying WHY. Excluded paths never participate in the graph and are
# never counted as coverage gaps. `node scripts/corpus-graph.mjs check` fails if
# an excluded path carries corpus frontmatter, or if an entry matches nothing.

# Highest-churn prepend-only event log — frontmatter would sit above the prepend
# point and churn with every entry; the log is a stream, not a page.
docs/context/log.md

# Append-only session rollup log — same stream class as log.md (session summaries
# are appended every meaningful session; frontmatter would fight the append point).
docs/context/operations/sessions/SESSION_LOG.md

# The pointer-based rehydration/bootstrap layer (BS-13): START_HERE + the Axium
# reinstantiation page (the pair CG-1 skipped) + the compaction handoff template.
# Deliberately carry no durable content — pointers only, designed never to rot.
docs/context/bootstrap/

# Verbatim pre-compaction snapshots of CURRENT_STATE + current_priorities

# Explicit archive layer: records of former prospective human collaborators.
# Frozen, no live relevance to any build surface.
docs/context/operations/archive/

# Pre planning scratch explicitly parked as overtaken-by-events — the
# directory name IS the status; tagging would only add lookup noise.
docs/OBE-docs/

# Generated artifact of this graph itself (regenerated by `index`); its sibling
# corpus-graph.json is not markdown and never scanned.
docs/context/corpus_graph_index_v0.md
