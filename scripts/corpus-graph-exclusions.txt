# corpus-graph exclusions — paths OUT-OF-UNIVERSE for the corpus graph.
# Created: 2026-08-25 (RR)
# One repo-relative path prefix or glob per line; each entry is preceded by a
# `#` comment saying WHY. Excluded paths never participate in the graph and are
# never counted as coverage gaps. `node scripts/corpus-graph.mjs check` fails if
# an excluded path carries corpus frontmatter, or if an entry matches nothing.

# Highest-churn prepend-only event log — the log is a stream, not a page.
docs/context/log.md

# Append-only session rollup log — same stream class as log.md.
docs/context/operations/sessions/SESSION_LOG.md

# Pointer-only rehydration/bootstrap layer — designed never to rot.
docs/context/bootstrap/

# TrAIdit's context root files kept as a read-only worked example.
docs/context.root-files.example/

# Raw RR source material — evidence, registered in evidence/raw/source_register.md,
# informative until synthesized. Not doctrine; not graph nodes.
docs/source-documents/

# Generated artifact of this graph itself (regenerated by `index`).
docs/context/corpus_graph_index_v0.md
