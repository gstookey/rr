/**
 * Public API of `@rr/windows` — the utility-window host (the Office host).
 *
 * AW-D10: recast the TrAIdit UWS mechanism, do not port it. S2's Device
 * inspector is the first Office to open as a window over any Suite.
 */
export { RR_WINDOWS_PACKAGE } from './lib/windows-package';
