{
  "name": "@rr/windows",
  "version": "0.0.0",
  "private": true,
  "description": "The utility-window host: the Office host mechanism (AW-D10, recast not ported).",
  "scripts": {
    "build": "ng build windows",
    "typecheck": "tsc --noEmit -p tsconfig.lib.json"
  },
  "peerDependencies": {
    "@angular/common": "^22.1.0",
    "@angular/core": "^22.1.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "sideEffects": false
}
