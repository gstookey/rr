{
  "name": "@rr/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Identity: the /api/me PermissionStore and CanMatchFn guard factories. The UI is never the enforcement point.",
  "scripts": {
    "build": "ng build auth",
    "typecheck": "tsc --noEmit -p tsconfig.lib.json"
  },
  "peerDependencies": {
    "@angular/common": "^22.1.0",
    "@angular/core": "^22.1.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "sideEffects": false
}
