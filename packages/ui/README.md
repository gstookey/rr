# @rr/ui

AstroUXDS facade + RR brand tokens. Presentational primitives only — MUST NOT import data-access or feature (Sheriff type:ui).

**S0 status:** stub. It builds, typechecks and exports its public API from `src/index.ts`; it has no behaviour yet. See `docs/design/packets/acme-workshop-01-design-packet/slice_decomposition_v0.md`.
