{
  "name": "@rr/store-features",
  "version": "0.0.0",
  "private": true,
  "description": "Shared NgRx SignalStore features (withRequestStatus, withMarkings, withEventStream) used by every Floor store.",
  "scripts": {
    "build": "ng build store-features",
    "typecheck": "tsc --noEmit -p tsconfig.lib.json"
  },
  "peerDependencies": {
    "@angular/common": "^22.1.0",
    "@angular/core": "^22.1.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "sideEffects": false
}
