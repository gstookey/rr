# Angular specific
dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
node_modules/
/yarn.lock
# NOTE (2026-09-04, ACME-WORKSHOP-01 S0): the ROOT package-lock.json is now
# COMMITTED. It was ignored while this repo held no application code; the RR
# npm workspace lives at the root from S0 on and `npm ci` — the local gate's
# first step and the island's only reproducible install — requires it.

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / archive junk
.DS_Store
__MACOSX/

# Legacy shell approximations (legacy-shells/*): package-lock.json IS committed;
# installs and build output are not.
legacy-shells/**/node_modules/
legacy-shells/**/dist/

# ACME Workshop runtime artefacts (S0) — docker-compose bind volumes
/infra/**/data/
